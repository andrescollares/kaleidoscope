# using doubles instead of i32 to avoid overflow

def sum([double] l) -> double: if l == [] then 0 else (head l) + sum(tail(l));

def one_to_n(double n) -> [double]:
    if n <= 0 then [] else n : one_to_n(n - 1);

sum(one_to_n(1000000.0));